id: ggplot2_py.ggplot
import_path: ggplot2_py.ggplot
kind: function
signature: ggplot(data=None, mapping=None)
purpose: start a new ggplot2 plot; create a layered graphic
description: 'Construct a GGPlot builder seeded with a data frame and default aesthetics.

  Subsequent `+` operations layer on geoms, stats, scales, facets, coords,

  and themes. The plot is not rendered until you save it with ggsave(path,

  plot=p) or display it in a Jupyter cell (_repr_png_); GGPlot has no .draw()

  or .save() method, and print(p) only emits a "<GGPlot ...>" summary.

  '
parameters:
- name: data
  type: DataFrame
  required: false
  default: null
  description: Default data for every layer; layers can override their own.
- name: mapping
  type: Mapping
  required: false
  default: null
  description: Default aes mapping; provided here so layers don't have to repeat it.
mutates: execution_class=builder; no declared in-place mutation; returns_kind=new_object
returns: GGPlot; kind=new_object
requires: []
writes: []
examples:
- ggplot(mpg, aes(x='displ', y='hwy')) + geom_point()
failure_fixes:
- when: AttributeError on attribute that looks like a column name (e.g. p.displ)
  suggest:
  - 'Aesthetic columns are strings inside aes(): aes(x=''displ''), not p.displ.'
- when: 'TypeError: aes() got positional argument'
  suggest:
  - 'Python aes() requires keyword args: aes(x=''col'', y=''col''), not aes(''col1'', ''col2'').'
- when: "AttributeError: 'GGPlot' object has no attribute 'save' (or 'ggsave')"
  suggest:
  - "GGPlot has no .save() method (that is plotnine's API, not this port). Save with the module-level function: from ggplot2_py import ggsave; ggsave('plot.png', plot=p, width=8, height=6, dpi=150)."
  explanation: ggplot2_py exposes ggsave(filename, plot=...) as a free function; the plot object's only public methods are add_build_hook and summary (fig_width / fig_height / fig_dpi are rendering-hint attributes, not methods).
related:
- ggplot2_py.geom_point
- ggplot2_py.geom_line
- ggplot2_py.geom_bar
- ggplot2_py.geom_smooth
- ggplot2_py.facet_wrap
